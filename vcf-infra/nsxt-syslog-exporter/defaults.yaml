# Field names follow the standard NSX-T Node API shape but haven't been
# exercised against a live NSX Manager — verify before real use.
exporter:
  name: loginsight
  server: loginsight.lab.local
  port: 514
  protocol: UDP
